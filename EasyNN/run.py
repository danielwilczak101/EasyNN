from examples.mnist import images, labels, show

#print(images[0])
#print(labels[0])

print(images.size)
print(labels.size)

show(images[2],labels[2])

# Tells me what the image is.
#model(images[0])
